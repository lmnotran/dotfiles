#!/bin/bash
# vim: set ft=bash:
#
# chezmoi run_once script - Install dependencies
# This runs once per machine (tracked by script hash)
#
# Delegates to script/install-deps for the actual work.
#
set -euo pipefail

exec "{{ .chezmoi.sourceDir }}/../script/install-deps"
