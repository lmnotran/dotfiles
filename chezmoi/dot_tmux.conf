# ~/.tmux.conf - Managed by chezmoi
#
# Sources gpakosz/.tmux (installed to ~/.tmux by chezmoi)
# Customizations go in ~/.tmux.conf.local

# Source the gpakosz/.tmux config
source-file ~/.tmux/.tmux.conf
