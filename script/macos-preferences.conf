# macOS preferences applied by script/bootstrap
# Format: <domain> <key> <type> <value>
# Type should be a valid defaults write flag (for example: -bool, -int, -float, -string)

com.apple.dock autohide-time-modifier -float 0.15
com.apple.dock autohide-delay -float 0
